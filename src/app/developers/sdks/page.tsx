import { Placeholder } from "@/components/placeholder";

export const metadata = { title: "SDK's" };

export default function Page() {
  return <Placeholder title={"SDK's"} eyebrow={"Developers"} />;
}
