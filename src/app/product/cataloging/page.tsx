import { Placeholder } from "@/components/placeholder";

export const metadata = { title: "Cataloging" };

export default function Page() {
  return <Placeholder title={"Cataloging"} eyebrow={"Product"} />;
}
