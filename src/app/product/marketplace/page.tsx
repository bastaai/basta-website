import { Placeholder } from "@/components/placeholder";

export const metadata = { title: "Marketplace" };

export default function Page() {
  return <Placeholder title={"Marketplace"} eyebrow={"Product"} />;
}
